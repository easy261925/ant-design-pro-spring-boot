import React from 'react';
import CalendarImage from '@/assets/calendar.jpg';

const CalendarWidget = () => {
  return <img style={{ width: '100%', height: '100%' }} src={CalendarImage} alt="" />;
};

export default CalendarWidget;
